#include <stdio.h>
int main()
{
    int input = 0;
    printf("study? 1 or 0 =>");
    scanf("%d",&input);
    if(input==1)
        printf("yes!");
    else
        printf("no!");
    return 0;
}
